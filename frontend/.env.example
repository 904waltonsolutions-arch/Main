# Base URL of the Walton Solutions backend API.
# Leave empty to call same-origin (e.g. when using a reverse proxy / rewrites).
# Example for local dev: http://localhost:5080
NEXT_PUBLIC_API_BASE_URL=
